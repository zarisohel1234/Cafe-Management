export class GlobalConstants{
    // Message
    public static genericError:any="Something went wrong ! Please try again later ";
    public static unauthorized:any="You are not Authorized Person";
    public static productExistError:any='Product already exist';
    public static productAdded:any='Product Added Successfully'
    // Regex
    public static nameRegex :any="[a-zA-Z0-9 ]*"
    public static emailRegex :any="[A-Za-z0-9._%-]+@[A-Za-z0-9._%-]+\\.[a-z]{2,3}"
    public static contactNumberRegex :any="^[e0-9]{10,10}$"

    // Variables
    public static error:any="error"
}